

const initialState = {
    spesaList: [],
    productList: [],
};

const fetchReducer = (state = initialState, action) => {
    switch (action.type) {

        default:
            return state;
    }
};

export default fetchReducer;

